fib ( numi a )
    return 0 if
    if 0 then
        return 0
    elif 1 then
        return 1
    else
        return fib ( a - 2 ) + fib ( a - 1 )
    //
//

main ()
    write fib (10)
//

boot ( main )

